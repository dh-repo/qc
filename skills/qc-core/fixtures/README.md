# Seeded fixtures

Tiny repos with planted defects. A port of a skill is incomplete until
`../scripts/verify_port.py` recovers `expected.json` from a ledger produced
against `repo/`.

Do not repair the planted defects in these trees.
