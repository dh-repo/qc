# qc-coherence report

Run: `2026-08-25T05:37:11Z-bec33ec`  
Scope: codebase (this pack)  
Prior state: hardening ledger (all findings fixed); invariants INV-VS-1, INV-JSON-1 enforced.

Prose verdict: **DRIFTING** → machine `READY_WITH_DEBT`.

## Sub-audits

| Unit | Result |
|------|--------|
| S1 stubs | Clean. `pass` in atomic_write unlink and JSON-schema loop is control flow, not unfinished work. Fixtures excluded. |
| S2 dead/dangling | Clean. ruff clean. CLI `main`/`if __name__` are entry points. Shims exec qc-core. |
| S3 duplication | **S3.1** deferred. Five fail-closed `json.loads` copies; no shared helper. Related F12.1. |
| S4 types | **S4.1** fixed (`type: ignore` comment). **S4.2** deferred (no mypy ratchet). |
| S5 unused deps | Skipped — no root pyproject/lockfile. |
| C1 conformance | No `Protocol`/`ABC` with 2+ implementations. JSON ingest is an implicit CLI contract, now fail-closed everywhere. |
| M1 naming | Cluster FAIL:/ERROR: written. Canonical form `FAIL:`. |
| M2 errors | **M2.1** fixed: append_history prefix aligned to `FAIL:`. |
| M3 serialization | No to_dict/from_dict surface. |
| M4 behavioral | N/A (no 2+ protocol backends). |
| M5 magic | Verdict enum and schema `1.1` recorded as canonical forms. |
| A1 direction | `layer_model` written: kernel ← cli ← shims; fixtures and js workflows isolated. qc.py imports stdlib only. |
| A2 boundaries | Scripts import sibling `qc.py`; no package `__init__` API. |
| A3 responsibility | No mixed-domain module over 500 LOC. verify_skills.py is one domain (skill-tree checks). |
| A4 invariants | INV-VS-1, INV-JSON-1 still enforced by tests. Harvested INV-VERDICT-1 (`check_single_compute_verdict`). |
| A5 cross-cutting | Same as M2.1. No logger/env/auth surface. |

## Dual-vote

| Id | Vote A | Vote B | Action |
|----|--------|--------|--------|
| M2.1 FAIL vs ERROR | real (semantic) | real (A5) | fix |
| S3.1 JSON ingest copies | real (clone) | uncertain (no remaining defect after fail-closed) | defer `accepted-debt` |
| S4.2 no types | mechanical | — | defer `accepted-debt` |
| S4.1 type: ignore | mechanical | — | fix (comment) |

## Maps written

- `canonical_forms[]`: cli-failure-prefix, json-ingest-failure, verdict, ledger-schema
- `layer_model`: kernel / cli / shims / fixtures / workflows
- `clusters[]`: CL-FAIL-PREFIX, CL-JSON-INGEST
- `deferred[]`: S3.1, S4.2
